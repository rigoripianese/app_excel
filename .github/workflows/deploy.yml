name: Auto Deploy to PythonAnywhere

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Webhook
        run: curl -X POST https://reus3111.pythonanywhere.com//webhook
